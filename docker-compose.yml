services:
  mediafetch:
    build: .
    container_name: mediafetch-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./downloads:/app/downloads
      - ./data:/app/data
    environment:
      - PORT=3000
      - HOST=0.0.0.0
